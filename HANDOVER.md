# گزارش جامع پروژه مانلی (Manneli Handover & Project Summary)

این سند خلاصه تمام اقدامات انجام‌شده، معماری فنی، راهنمای انتقال به سیستم جدید و پرامپت آماده برای ادامه کار در سیستم یا چت جدید است.

---

## ۱. معرفی و مشخصات پروژه
- **نام پروژه:** مانلی (Manneli)
- **زمینه فعالیت:** فروشگاه آنلاین تخصصی لوازم و تجهیزات ناخن (Nail Art)، دوره‌های آموزشی مدرسین و شبکه نمایندگی‌های سراسر کشور.
- **تکنولوژی:** استاتیک خالص (Pure Vanilla HTML5 / CSS3 / Vanilla JavaScript ES6+) — بدون نیاز به Node.js، Webpack، React یا ابزارهای بیلد کامپایلری.
- **مخزن گیت‌هاب (GitHub Repository):**
  `https://github.com/aeashkan/Manneli.git`

---

## ۲. ساختار فایل‌ها و پوشه‌ها (`public/`)

```text
public/
├── index.html              # صفحه اصلی و ساختار تک‌صفحه‌ای (SPA) سایت (v=2.7)
├── README.md               # راهنمای اولیه
├── HANDOVER.md             # سند انتقال کامل پروژه (همین فایل)
├── css/
│   ├── base.css            # متغیرهای CSS، ریست استایل، تعریف فونت‌ها و تم‌ها
│   ├── layout.css          # گریدبندی کلی، هدر، فوتر، سایدبار و ناوبری
│   ├── components.css      # کارت‌های محصول، نقشه، PDP، تب‌ها، فیلترها و دکمه‌ها
│   └── overlays.css        # مودال‌های ورود، ثبت‌نام، سبد خرید، اعلانات و منوی همبرگری
├── js/
│   ├── data.js             # دیتابیس لوکال محصولات، دسته‌بندی‌ها، اساتید و کامنت‌ها
│   ├── iran-map-data.js    # دیتای پین‌ها، مسیرهای وکتوری SVG استان‌های ایران
│   ├── ui.js               # توابع تم، سوییچر دارک/لایت، توست‌ها، اسکرول و منوی موبایل
│   ├── catalog.js          # فیلتر محصولات، رندر کاتالوگ و صفحه جزئیات کالا (PDP)
│   ├── cart.js             # مدیریت سبد خرید، تخفیف، تسویه‌حساب و ذخیره آدرس
│   ├── account.js          # حساب کاربری، تاریخچه سفارش‌ها، فاکتور و مدیریت آدرس‌ها
│   ├── b2b.js              # بخش نقشه نمایندگی‌ها، پنل مدرسین و فرم‌های همکاران
│   └── main.js             # راه‌اندازی اولیه و اتصال ایونت‌لیسنرها
├── Fonts/
│   ├── Dana-Medium.ttf     # فونت دانا (فونت اصلی متن و فرم‌ها)
│   └── Morabba-SemiBold.ttf# فونت مربا (عناوین و تیترهای لوکس)
└── Logo/
    ├── DarkLogo.png        # لوگوی اختصاصی مانلی برای تم دارک
    └── LightLogo.png       # لوگوی اختصاصی مانلی برای تم لایت
```

---

## ۳. دیزاین سیستم و پالت رنگی مانلی

- **تم دارک (پیش‌فرض):**
  - پس‌زمینه اصلی: `#18130E` و `#241C14`
  - رنگ کارت‌ها: `#2D2319` و `#362A1F`
  - متون: کرم روشن `#F5EEE4` و سفید عاجی `#FAF6F0`
- **تم لایت (Luxury Warm Minimal):**
  - پس‌زمینه سایت: کرم نود گرم `#FBF7F0`
  - کارت‌های اصلی و PDP: شکلاتی تیره لوکس مانلی `#382E24` با متون درخشان `#FAF6F0` (جلوگیری از سفید روی سفید و حفظ خوانایی ۱۰۰٪)
  - پس‌زمینه‌های تو در تو: `#2C241A` و `#2D2319`
- **رنگ‌های سازمانی (Accent):**
  - طلایی برنز مانلی: `#B38E5D` و `#C5A26E`
  - هاور دکمه‌ها و عناصر ویژه: `#9E7A49`
- **تایپوگرافی:**
  - فونت عناوین: `Morabba`
  - فونت بدنه و قیمت‌ها: `Dana`
  - فونت لاتین لوگو: `Playfair Display`

---

## ۴. کارهای انجام‌شده (خلاصه ۴۶ تسک پیاده‌سازی‌شده)

1. **معماری و ساختاربندی:**
   - تفکیک فایل تکین CSS به ۴ فایل مجزای ماژولار (`base`, `layout`, `components`, `overlays`).
   - تفکیک فایل تکین JS به ۷ فایل حوزه‌ای تخصصی با بارگذاری تمیز `defer`.
2. **نقشه تعاملی ایران و نمایندگی‌ها:**
   - استخراج مختصات و مسیرهای SVG استان‌های کشور و جای‌گذاری پین‌های تعاملی.
   - چیدمان دو ستونه دسکتاپ (نقشه + کارت‌های نمایندگان) و ریسپانسیو موبایل.
   - اتصال جستجوی لحظه‌ای استان/شهر میان لیست و نقشه با باز شدن مودال اطلاعات نماینده.
3. **احراز هویت و حساب کاربری (مشابه دیجی‌کالا):**
   - مودال هوشمند ورود با پیامک / رمز عبور / ثبت‌نام بدون فیلدهای اضافه.
   - پنل کاربری جامع با تب‌های اطلاعات حساب، سفارش‌ها، جزییات فاکتور، مدیریت آدرس‌ها و تغییر رمز.
   - ورود اساتید با رمز عبور و گزینه دریافت رمز یک‌بارمصرف.
4. **سبد خرید و تسویه‌حساب (Checkout):**
   - فرایند گام‌به‌گام تکمیل اطلاعات و انتخاب آدرس ذخیره‌شده یا افزودن آدرس جدید.
   - مودال خرید حضوری از فروشگاه‌ها و تعیین نمایندگی شهر.
5. **صفحه جزئیات محصول (PDP):**
   - گالری تصاویر اسلایدر/بندانگشتی و زوم.
   - جعبه خرید (Buybox) با انتخاب رنگ/سایز و نوار چسبان (Sticky Bar) برای موبایل.
   - تب‌های نقد و بررسی، جدول مشخصات فنی و بازطراحی دیدگاه‌ها به سبک دیجی‌کالا.
   - امکان باز/بسته کردن ویژگی‌های برجسته کالا.
6. **برندینگ و ریسپانسیو:**
   - فونت‌های محلی دانا و مربا در پروژه گنجانده و لود شدند.
   - سیستم سوییچ دارک/لایت با جایگزینی خودکار لوگوهای `DarkLogo.png` و `LightLogo.png`.
   - منوی همبرگری انیمیشنی برای موبایل با دراور اسلایدی راست‌به‌چپ.
   - پالایش دیتابیس کاتالوگ و حذف کالاهای فاقد تصویر.
7. **رفع باگ‌ها و کش استاتیک:**
   - رفع کامل ناخوانایی صفحه حساب کاربری در حالت لایت‌مود.
   - رفع کامل ناخوانایی صفحه جزئیات محصول در حالت لایت‌مود با کارت‌های شکلاتی و متون کرم.
   - اعمال نسخه کش‌شکن `?v=2.7` برای تمام منابع استاتیک در `index.html`.

---

## ۵. راهنمای اجرا و ادامه روی سیستم جدید

### روش ۱: با استفاده از Git (پیشنهادی)
```bash
git clone https://github.com/aeashkan/Manneli.git
cd Manneli
```

### روش ۲: کپی دستی فایل‌ها
کل محتویات پوشه `public/` را به سیستم جدید منتقل کنید.

### اجرای سایت:
- کافیست فایل `index.html` را در هر مرورگری باز کنید.
- یا برای اجرای لوکال با سرور سبک:
```bash
# با استفاده از پایتون:
python -m http.server 3000

# یا با npx:
npx serve .
```

---

## ۶. پرامپت آماده برای شروع در سیستم یا هوش مصنوعی جدید

کافیست متن زیر را کپی کرده و در اولین پیام به هوش مصنوعی در سیستم جدید ارسال کنید:

> «سلام، من در حال توسعه وب‌سایت مانلی (Manneli) هستم؛ یک فروشگاه آنلاین ناخن با معماری Pure Vanilla HTML/CSS/JS که مخزن آن در گیت‌هاب قرار دارد. فایل HANDOVER.md در ریشه پروژه قرار دارد که جزییات کامل ۴۶ تسک پیاده‌سازی‌شده، پالت رنگی تم دارک و لایت مانلی و ساختار فایل‌های CSS و JS را توضیح داده است. آخرین وضعیت پروژه کاملاً پایدار، با کش‌شکن نسخه v=2.7 و با رفع باگ‌های لایت‌مود PDP و حساب کاربری است. لطفاً فایل HANDOVER.md را مطالعه کن و بگو چطور می‌توانیم مراحل بعدی را پیش ببریم.»
